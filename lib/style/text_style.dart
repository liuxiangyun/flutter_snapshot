import 'package:flutter/material.dart';

///白色背景使用的灰色文字---start
///
///标准 87% alpha
const greyTextColorPrimary = Color(0xDE000000);

///视觉层次偏低的次要文字 54% alpha
const greyTextColorLight = Color(0x8A000000);

///像正文和标签中用于提示用户的文字，视觉层次更低 26% alpha
const greyTextColorLightPlus = Color(0x42000000);

const double fontSize_14=14.0;

const greyTextStylePrimary =
    TextStyle(color: greyTextColorPrimary, fontSize: fontSize_14);

const greyTextStyleLight = TextStyle(color: greyTextColorLight, fontSize:fontSize_14);

const greyTextStyleLightPlus =
    TextStyle(color: greyTextColorLightPlus, fontSize: fontSize_14);

///
///白色背景使用的灰色文字---end
