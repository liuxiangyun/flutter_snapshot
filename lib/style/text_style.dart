import 'package:flutter/material.dart';
import 'color.dart';
import 'font_size.dart';

///白色背景使用的灰色文字---start
const greyFontSize = fontSize14;
const greyTextStylePrimary =
    TextStyle(color: greyTextColorPrimary, fontSize: greyFontSize);

const greyTextStyleLight =
    TextStyle(color: greyTextColorLight, fontSize: greyFontSize);

const greyTextStyleLightPlus =
    TextStyle(color: greyTextColorLightPlus, fontSize: greyFontSize);

///白色背景使用的灰色文字---end
